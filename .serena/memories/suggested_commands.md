# Suggested Commands

- `bun run dev` - Start dev server at localhost:5176
- `bun run build` - Production build
- `bun run check` - Type-check SvelteKit app
- `bun run dev:services` - Start Docker services
- `git status` / `git diff` / `git log` - standard git
